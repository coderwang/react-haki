import React from 'react';
import { RippleButton } from 'react-haki';

export default () => {
  return (
    <RippleButton clickStatus="loading" disabled>
      RippleButton
    </RippleButton>
  );
};
