import React from 'react';
import { RippleButton } from 'react-haki';

export default () => {
  return (
    <RippleButton clickStatus="success" disabled>
      RippleButton
    </RippleButton>
  );
};
